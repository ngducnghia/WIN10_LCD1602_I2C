<Page
    x:Class="LCD_I2C.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LCD_I2C"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <TextBlock Name="status_TB" Text="Status: " 
                       FontSize="50" Margin="20" TextWrapping="Wrap"/>
            <TextBlock Text="Input text below to show on LCD: " 
                       FontSize="50" Margin="20" TextWrapping="Wrap"/>
            <TextBox Name="input_TB" FontSize="50" Margin="20" Text="Sample Text" 
                     AcceptsReturn="False" />
            <Button Name="sendToLCD" Content="Send String to LCD" FontSize="50" 
                    Margin="20" Click="sendToLCD_Click" HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</Page>
